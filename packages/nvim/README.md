# Neovim

Requires Neovim 0.12+. Plugins are managed by `vim.pack`; blink.cmp and blink.lib
follow their default branches without version, branch, or commit constraints.
The local pack lockfile remains ignored. Updates are applied with `vim.pack.update()`.

On first launch or after updating blink.cmp, Cargo builds the Rust completion
matcher if a matching library is not already available. If Cargo is unavailable
or the build fails, completion falls back to Lua. Restart Neovim after fixing
the toolchain to retry.

## Language tools

Language servers and formatters must be on `PATH`; Mason is not required.

| Language | Language server | Formatter |
| --- | --- | --- |
| Lua | `lua-language-server` | `stylua` |
| Python | `basedpyright-langserver` + `ruff server` | `ruff format` |
| Rust | `rust-analyzer` | `rustfmt` |
| Swift | `sourcekit-lsp` (Xcode / Swift toolchain) | `swift-format` |
| Dart / Flutter | `dart language-server` (Dart / Flutter SDK) | `dart format` |
| Shell | None configured | `shfmt` |

The Homebrew-managed tools are listed in `scripts/Brewfile`. When using rustup,
install its components with `rustup component add rust-analyzer rustfmt`.
For Xcode projects, SourceKit may need a project-specific `buildServer.json`
generated by `xcode-build-server`; Flutter uses the active Dart SDK on `PATH`.

Conform formats on save, preferring the configured formatter and falling back
to LSP formatting when no external formatter is available. Project formatter
configuration is respected. Python import organization is available through
Ruff code actions and does not run automatically on save.

Use `:FormatDisable` / `:FormatEnable` for the global autoformat switch. Add `!`
to change only the current buffer's switch. Either disabled switch prevents
autoformat; manual formatting still works.

## Tree-sitter highlighting

Tree-sitter highlighting starts automatically for every filetype with an installed
parser and highlight queries. Supported parsers are installed asynchronously on
first use, then highlighting starts in matching open buffers. Installation requires
`tree-sitter` CLI 0.26.1+ and a C compiler. Unsupported languages and pending or
failed installations retain Vim syntax highlighting. Failed downloads are attempted
only once per language per session; retry with `:TSInstall <language>` and reopen
the file. Run `:TSUpdate` after updating nvim-treesitter to keep parsers and queries
compatible.

Use `:Inspect` on code to check Tree-sitter captures.

## Keys

`<leader>` is Space.

| Keys | Action |
| --- | --- |
| `gd`, `gD`, `gI`, `gy` | Definition, declaration, implementation, type definition |
| `grr` | References |
| `K` | Hover documentation |
| `<leader>ca`, `<leader>cr`, `<leader>cs` | Code action, rename, document symbols |
| `<leader>cf` | Format buffer or visual selection |
| `<leader>cd` | Current-line diagnostics in a float |
| `[d`, `]d` | Previous / next diagnostic (Neovim defaults) |
| `<leader>cw` | Populate diagnostics for unopened workspace files |
| `<leader>xx`, `<leader>xd` | Search workspace / current-buffer diagnostics |
| `<leader>xt` | Toggle inline diagnostic messages |
| `<leader>sn` | Search notification history |
| `Ctrl-Space` | Open completion menu / documentation |
| `Ctrl-n`, `Ctrl-p` | Select next / previous completion |
| `Ctrl-y`, `Ctrl-e` | Accept / cancel completion |
| `Tab`, `Shift-Tab` | Move between snippet placeholders in insert mode |
| `Ctrl-k` | Toggle signature help in insert mode |

## Diagnostics and command line

Tiny Inline Diagnostic displays inline messages; native virtual text and virtual
lines are disabled to avoid duplicates. Signs, underlines, and lualine counts
remain available.

`:WorkspaceDiagnostics` (also `<leader>cw`) uses native workspace diagnostics
when the server supports them. Otherwise, workspace-diagnostics.nvim scans
readable Git-tracked and unignored new files under the LSP root, once per client.
Each client gets a separate file cache. Only its configured filetypes are sent
to that server. Run `:lsp restart` before rescanning newly created files. Large
projects can take time to analyze; results arrive asynchronously and can be
browsed with `<leader>xx`. The fallback scan requires a Git workspace.

Tiny Cmdline uses native `ui2` for a centered command window, with blink.cmp
completion aligned below it. `/` and `?` searches stay at the bottom. Hover
documentation uses Neovim's built-in UI; blink.cmp provides signature help.

nvim-notify handles `vim.notify` with compact notifications in the top-right
corner that fade out after three seconds. It uses Tokyonight's background color
for animations with the transparent theme. Use `<leader>sn` to search notification
history, `:Notifications` to display it, or `:NotificationsClear` to clear it.

Fidget shows LSP progress in a transparent window in the bottom-right corner.
Completed tasks disappear after three seconds. General notifications continue
to use nvim-notify. Use `:Fidget clear` to dismiss active progress messages.

For troubleshooting, use `:checkhealth vim.lsp`, `:checkhealth blink.cmp`,
`:ConformInfo`, and `:messages`.
